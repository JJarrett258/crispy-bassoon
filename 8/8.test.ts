let adventOfCodeExampleInput = 
`nop +0
acc +1
jmp +4
acc +3
jmp -3
acc -99
acc +1
jmp -4
acc +6`

// test('Should return an output of 5 when using advent of code data', () => {
//     expect(toBeDetermined(adventOfCodeExampleInput)).toEqual(5)
// })
// import { splitIntructionIntoConstituentParts, parseInitialBootCode } from './8-solution'
// test('Should print out the correct string according to the regex used', () => {
//     expect(parseInitialBootCode(adventOfCodeExampleInput))
// })